import { environment } from '../environments/environment';

// API Endpoints
export const apiUrlVideo = environment.apiUrl;
export const apiUrlPlaylist = `${environment.apiUrl}/playlist`;
export const apiUrlDash = `${environment.apiUrl}/dash`;
